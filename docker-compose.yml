services:
  dataflow-sentinel:
    build: .
    container_name: dataflow-sentinel

    network_mode: "host"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - SENTRY_DSN=${SENTRY_DSN}
    volumes:
      - ./data:/app/data
